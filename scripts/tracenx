#!/bin/bash

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
TRACENX_DIR="${SCRIPT_DIR}/../../TRACEN-X"

echo "running tracenx script..."

cd "${TRACENX_DIR}" || exit 1
. run.sh